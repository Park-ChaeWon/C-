#include <stdio.h>
int h[100001];
int o[100001];
int  main(void){
	int l, d, n;
	int i;
	
	scanf("%d", &n); //총 레일의 개수 입력
	for(i=1;i<=n;i++){
		scanf("%d%d",&h[i], &o[i]); //각 레일의 시작점과 도착점 입력 
	}
	scanf("%d", &d); //새로 만들 기차의 길이 입력
	
	 
	
}
